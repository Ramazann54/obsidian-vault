

---

## 1. Зачем ОС управляет памятью

Память — **ограниченный и критически важный ресурс**.  
ОС должна:

- безопасно распределять память между процессами;
    
- изолировать процессы друг от друга;
    
- эффективно использовать доступный объём;
    
- обеспечивать удобную модель программирования.
    

📌 Без управления памятью:

- программы перезаписывали бы друг друга;
    
- система была бы нестабильной;
    
- невозможна была бы многозадачность.
    

---

## 2. Основные функции ОС по управлению памятью

---

### 2.1. Распределение и освобождение памяти

ОС:

- выделяет память процессу при создании;
    
- освобождает её при завершении;
    
- может динамически изменять объём (heap, stack).
    

Используются:

- таблицы распределения;
    
- битовые карты;
    
- списки свободных блоков.
    

---

### 2.2. Адресация памяти

ОС предоставляет программам **абстракцию адресного пространства**:

- процесс «видит» свою память как непрерывную;
    
- физически она может быть разбросана.
    

👉 Это основа **виртуальной памяти**.

---

### 2.3. Защита памяти

ОС предотвращает:

- доступ процесса к памяти другого процесса;
    
- запись в код;
    
- обращение к невыделенной памяти.
    

Реализуется через:

- аппаратные механизмы (MMU);
    
- права доступа (r/w/x).
    

---

### 2.4. Изоляция процессов

Каждый процесс имеет:

- собственное виртуальное адресное пространство;
    
- собственные таблицы страниц.
    

📌 Ошибка одного процесса **не ломает всю систему**.

---

### 2.5. Поддержка виртуальной памяти

ОС:

- позволяет использовать больше памяти, чем физически доступно;
    
- выгружает неиспользуемые страницы на диск (swap);
    
- загружает их по требованию (page fault).
    

---

### 2.6. Перемещение и компактизация

ОС может:

- перемещать блоки памяти;
    
- устранять фрагментацию;
    
- оптимизировать размещение процессов.
    

(Актуально для сегментных и старых моделей.)

---

### 2.7. Совместное использование памяти

ОС позволяет:

- разделять память между процессами;
    
- использовать shared memory;
    
- эффективно реализовывать IPC.
    

---

## 3. Типы адресов памяти

---

### 3.1. Логический (виртуальный) адрес

📌 **Адрес, который использует программа**.

- формируется CPU;
    
- существует в рамках виртуального адресного пространства процесса;
    
- одинаковые адреса у разных процессов могут указывать на разные данные.
    

Пример:

int x;  // x может быть по адресу 0x400123

---

### 3.2. Физический адрес

📌 **Реальный адрес в оперативной памяти**.

- используется контроллером памяти;
    
- недоступен напрямую пользовательским программам;
    
- уникален для всей системы.
    

---

### 3.3. Виртуальный адрес

Часто используется как синоним логического адреса, но точнее:

- логический адрес → адрес в логике программы;
    
- виртуальный адрес → адрес после сегментации и страничной трансляции.
    

(На экзамене обычно их **не разделяют строго**.)

---

### 3.4. Относительный адрес

- адрес относительно начала сегмента или программы;
    
- используется на этапе компиляции и загрузки.
    

---

### 3.5. Абсолютный адрес

- окончательный адрес в памяти;
    
- используется редко в современных ОС;
    
- характерен для простых или встроенных систем.
    

---

## 4. Преобразование адресов (address translation)

Процесс преобразования:

Логический адрес  
        ↓  
MMU (таблицы страниц / сегментов)  
        ↓  
Физический адрес

📌 ОС управляет таблицами,  
📌 аппаратно преобразует MMU.

---

## 5. Роль аппаратной поддержки

Для управления памятью используются:

- MMU (Memory Management Unit);
    
- регистры базы и предела;
    
- таблицы страниц;
    
- TLB (кэш трансляций).
    

---

## 6. Почему программы не видят физическую память

Потому что:

- это небезопасно;
    
- нарушает изоляцию;
    
- делает ОС нестабильной.
    

📌 Только ядро работает с физическими адресами напрямую.

---

## 7. Связь с многозадачностью

Управление памятью позволяет:

- выполнять несколько программ одновременно;
    
- не мешать друг другу;
    
- эффективно использовать ОЗУ.
    

---

## 8. Экзаменационная формулировка

> Операционная система выполняет функции распределения, защиты и управления памятью, предоставляя процессам изолированные виртуальные адресные пространства.  
> Для этого используются различные типы адресов: логические (виртуальные), формируемые программами, и физические, соответствующие реальным ячейкам оперативной памяти, между которыми осуществляется аппаратно поддерживаемое преобразование адресов.

---

