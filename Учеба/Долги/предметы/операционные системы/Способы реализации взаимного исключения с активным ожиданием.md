## 1. Что такое активное ожидание

**Активное ожидание (busy waiting)** — это способ синхронизации, при котором процесс или поток **не блокируется**, а постоянно проверяет условие входа в критическую область, выполняя пустые или проверочные инструкции.

Проще:

> поток **крутится в цикле**, пока не получит доступ к ресурсу.

---

### Отличие от пассивного ожидания

- **Активное ожидание**  
    → поток потребляет процессорное время
    
- **Пассивное ожидание**  
    → поток блокируется и снимается с планирования
    

---

## 2. Когда используется активное ожидание

Активное ожидание оправдано, если:

- время ожидания очень короткое;
    
- переключение контекста дороже, чем ожидание;
    
- используется на уровне ядра;
    
- применяется в многопроцессорных системах.
    

---

## 3. Основные способы реализации взаимного исключения

## с активным ожиданием

---

## 3.1. Запрет прерываний

### Идея

Процесс запрещает прерывания перед входом в критическую область:

- другие процессы не смогут быть запланированы;
    
- выполнение становится атомарным.
    

### Пример (логически)

disable_interrupts  
    критическая область  
enable_interrupts

### Недостатки

- работает **только на одном процессоре**;
    
- опасно: можно «заморозить» систему;
    
- применяется **только в ядре ОС**.
    

---

## 3.2. Простая блокировка (флаг)

### Идея

Используется общая переменная `lock`:

- `lock = 0` — свободно
    
- `lock = 1` — занято
    

while (lock == 1);   // активное ожидание  
lock = 1;  
  
// критическая область  
  
lock = 0;

### Проблема

❌ **Не гарантирует взаимное исключение**, так как проверка и установка `lock` не атомарны.

---

## 3.3. Строгое чередование

### Идея

Процессы по очереди входят в критическую область.

Используется переменная `turn`:

- `turn = 0` → процесс 0
    
- `turn = 1` → процесс 1
    

while (turn != i);  // активное ожидание  
  
// критическая область  
  
turn = 1 - i;

### Недостатки

- нарушает принцип прогресса;
    
- процесс может ждать, даже если ресурс свободен.
    

---

## 3.4. Алгоритм Петерсона

### Идея

Комбинация:

- флагов готовности
    
- переменной очередности
    

Для **двух процессов**.

flag[i] = true;  
turn = j;  
  
while (flag[j] && turn == j);  
// критическая область  
flag[i] = false;

### Свойства

✔️ взаимное исключение  
✔️ отсутствие deadlock  
✔️ отсутствие starvation  
❌ плохо масштабируется  
❌ редко используется на практике

---

## 3.5. Атомарные операции

### Идея

Используются **аппаратные инструкции**, которые выполняются атомарно:

- Test-and-Set
    
- Compare-and-Swap
    
- Fetch-and-Add
    

Пример `Test-and-Set`:

while (test_and_set(lock));  
// критическая область  
lock = 0;

### Преимущества

- корректная реализация взаимного исключения;
    
- основа для спинлоков;
    
- широко используется в ядрах ОС.
    

---

## 3.6. Спинлоки (Spinlocks)

### Идея

Спинлок — это блокировка с активным ожиданием:

- поток «крутится», пока не освободится замок;
    
- реализуется через атомарные операции.
    

### Особенности

- эффективны при коротких критических секциях;
    
- используются в многопроцессорных системах;
    
- активно применяются в ядре ОС.
    

---

## 4. Недостатки активного ожидания

- расход процессорного времени;
    
- ухудшение производительности;
    
- неэффективно при длительном ожидании;
    
- опасно в пользовательском коде.
    

---

## 5. Где применяется активное ожидание

- в ядре операционной системы;
    
- в драйверах;
    
- в многопроцессорных системах;
    
- при очень коротких критических областях.
    

---

## 6. Связь с последующими темами

Активное ожидание — основа для:

- спинлоков;
    
- атомарных операций;
    
- низкоуровневой синхронизации.
    

Пассивное ожидание (мьютексы, семафоры) строится **поверх этих механизмов**.

---

## 7. Короткая экзаменационная формулировка

> Активное ожидание — это способ реализации взаимного исключения, при котором процесс или поток не блокируется, а циклически проверяет условие входа в критическую область.  
> Основными методами реализации являются запрет прерываний, использование флагов и переменных очередности, алгоритм Петерсона, атомарные операции и спинлоки.  
> Активное ожидание эффективно при коротком времени ожидания и используется преимущественно в ядре операционной системы.

---

Если хочешь, следующим логично разобрать:

- **семафоры**
    
- **мьютексы**
    
- **проблему производителя и потребителя**
    
- или сравнение активного и пассивного ожидания одной таблицей
    

Куда идём дальше? 💪