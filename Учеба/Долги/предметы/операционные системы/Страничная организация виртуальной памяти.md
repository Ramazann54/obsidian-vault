

---

## 1. Зачем нужна страничная организация

Основная цель — **избавиться от внешней фрагментации** и упростить управление памятью.

📌 Идея:  
разбить память **не по логике программы**, а на **блоки фиксированного размера**.

---

## 2. Основное определение

**Страничная организация виртуальной памяти** — это способ управления памятью, при котором:

- виртуальное адресное пространство процесса разбивается на **страницы фиксированного размера**;
    
- физическая память разбивается на **кадры (frames)** того же размера;
    
- страница может быть размещена **в любом свободном кадре**.
    

---

## 3. Виртуальный адрес при страничной организации

Виртуальный адрес состоит из двух частей:

<номер страницы, смещение>

- номер страницы — индекс в таблице страниц;
    
- смещение — позиция внутри страницы.
    

📌 Размер страницы задаётся аппаратно (обычно 4 КБ).

---

## 4. Таблица страниц

Для каждого процесса ОС хранит **таблицу страниц**, где для каждой страницы указано:

- номер физического кадра;
    
- бит присутствия в памяти;
    
- биты доступа (R/W/X);
    
- бит обращения;
    
- бит модификации.
    

---

## 5. Преобразование виртуального адреса

Алгоритм доступа к памяти:

виртуальный адрес  
   ↓  
номер страницы  
   ↓  
таблица страниц  
   ↓  
номер кадра + смещение  
   ↓  
физический адрес

Если страница отсутствует в памяти → **page fault**.

---

## 6. Страничное прерывание (page fault)

**Page fault** возникает, если:

- нужная страница находится на диске;
    
- страница ещё не загружена.
    

Действия ОС:

1. остановить процесс;
    
2. выбрать страницу для вытеснения;
    
3. при необходимости сохранить её на диск;
    
4. загрузить нужную страницу;
    
5. обновить таблицу страниц;
    
6. продолжить процесс.
    

---

## 7. Виртуальная память и подкачка

📌 Страничная организация **неразрывно связана с виртуальной памятью**:

- не вся программа находится в ОЗУ;
    
- страницы загружаются по требованию;
    
- используется файл подкачки (swap).
    

---

## 8. Фрагментация памяти

При страничной организации:

- ❌ внешней фрагментации нет;
    
- ⚠️ возможна **внутренняя фрагментация**  
    (последняя страница может быть заполнена не полностью).
    

---

## 9. Аппаратная поддержка

Для ускорения работы используются:

- MMU (блок управления памятью);
    
- TLB (кэш трансляции адресов).
    

📌 Без TLB доступ был бы слишком медленным.

---

## 10. Преимущества

- отсутствие внешней фрагментации;
    
- простота размещения страниц;
    
- удобная подкачка;
    
- хорошая поддержка виртуальной памяти.
    

---

## 11. Недостатки

- внутренняя фрагментация;
    
- накладные расходы на таблицы страниц;
    
- необходимость аппаратной поддержки;
    
- сложные алгоритмы замещения страниц.
    

---

## 12. Многоуровневые таблицы страниц

Чтобы экономить память:

- таблицы страниц делятся на уровни;
    
- используются только нужные части;
    
- виртуальное адресное пространство может быть очень большим.
    

---

## 13. Где используется

- современные ОС: Linux, Windows, macOS;
    
- основа виртуальной памяти в большинстве систем.
    

---

## 14. Сравнение с сегментацией

|Критерий|Страницы|Сегменты|
|---|---|---|
|Размер|фиксированный|переменный|
|Фрагментация|внутренняя|внешняя|
|Логика программы|❌|✅|
|Реализация|проще|сложнее|

---

## 15. Экзаменационная формулировка

> Страничная организация виртуальной памяти — это способ управления памятью, при котором виртуальное адресное пространство процесса разбивается на страницы фиксированного размера, отображаемые в физические кадры с помощью таблицы страниц.  
> Данный подход устраняет внешнюю фрагментацию и позволяет эффективно реализовать виртуальную память с подкачкой страниц.
