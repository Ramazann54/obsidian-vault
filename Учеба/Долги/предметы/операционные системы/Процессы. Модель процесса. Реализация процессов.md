# **Процессы. Модель процесса. Реализация процессов**

---

## 1. Что такое процесс

### 1.1. Определение

> **Процесс** — это экземпляр выполняемой программы вместе с её текущим состоянием, ресурсами и контекстом выполнения.

Важно:

- **программа** — это пассивный файл на диске
    
- **процесс** — активная сущность в памяти
    

Одна программа → много процессов  
(например, несколько окон браузера)

---

### 1.2. Зачем ОС ввела понятие процесса

Процесс нужен, чтобы ОС могла:

- выполнять несколько программ «одновременно»
    
- изолировать программы друг от друга
    
- управлять ресурсами (CPU, память, файлы)
    
- планировать выполнение
    

👉 Процесс — **единица управления и защиты ресурсов**.

---

## 2. Модель процесса

Модель процесса — это **абстракция**, с помощью которой ОС описывает выполнение программ.

---

### 2.1. Процесс как виртуальный процессор

Каждому процессу кажется, что:

- у него **свой CPU**
    
- он выполняется непрерывно
    
- другие процессы не существуют
    

На самом деле:

- CPU один (или несколько)
    
- ОС быстро переключает процессы
    

👉 Это **иллюзия параллелизма**.

---

### 2.2. Состояния процесса

Классическая **трёхсостоянийная модель**:

1. **Running (выполняется)**  
    Процесс сейчас на CPU
    
2. **Ready (готов)**  
    Может выполняться, но ждёт процессор
    
3. **Blocked / Waiting (ожидает)**  
    Ждёт событие (ввод-вывод, сигнал)
    

Ready → Running → Blocked  
   ↑         ↓  
   ←─────────

---

### 2.3. Расширенная модель состояний

В реальных ОС добавляют:

- New — создан, но не запущен
    
- Terminated — завершён
    
- Suspended Ready
    
- Suspended Blocked
    

Это нужно для:

- подкачки
    
- управления памятью
    
- приостановки процессов
    

---

## 3. Атрибуты процесса

Каждый процесс в ОС имеет:

### 3.1. Контекст процессора

- счётчик команд (PC)
    
- регистры
    
- указатель стека
    
- флаги
    

---

### 3.2. Контекст процесса (ресурсы)

- виртуальное адресное пространство
    
- стек и куча
    
- открытые файлы
    
- дескрипторы устройств
    
- приоритет
    
- идентификаторы (PID, PPID)
    
- права доступа
    

---

## 4. Реализация процессов в ОС

### 4.1. PCB — Process Control Block

Процесс в ОС представлен структурой данных:

> **PCB (блок управления процессом)** — структура, в которой ОС хранит всю информацию о процессе.

Типичное содержимое PCB:

- PID
    
- состояние
    
- приоритет
    
- указатель на таблицы страниц
    
- сохранённый контекст CPU
    
- список открытых файлов
    
- учёт времени CPU
    

👉 **PCB — это и есть процесс с точки зрения ОС**.

---

### 4.2. Создание процесса

Основные шаги:

1. Выделение PID
    
2. Создание PCB
    
3. Выделение адресного пространства
    
4. Загрузка программы в память
    
5. Инициализация стека
    
6. Добавление в очередь Ready
    

В Unix-подобных системах:

- `fork()` — копирование процесса
    
- `exec()` — загрузка новой программы
    

---

### 4.3. Завершение процесса

Процесс может завершиться:

- нормально (exit)
    
- по ошибке
    
- по сигналу
    
- принудительно (kill)
    

ОС при этом:

- освобождает память
    
- закрывает файлы
    
- удаляет PCB
    

---

## 5. Переключение контекста

### 5.1. Что это

> **Переключение контекста** — это сохранение состояния одного процесса и восстановление состояния другого.

---

### 5.2. Когда происходит

- истёк квант времени
    
- процесс заблокировался
    
- пришло прерывание
    
- более приоритетный процесс стал готовым
    

---

### 5.3. Этапы

1. Сохранить регистры текущего процесса в PCB
    
2. Изменить его состояние
    
3. Выбрать новый процесс
    
4. Загрузить его контекст
    
5. Продолжить выполнение
    

---

## 6. Процессы и потоки (важно понимать разницу)

|Процесс|Поток|
|---|---|
|Свой адрес|Общее адресное пространство|
|Тяжёлый|Лёгкий|
|Медленное переключение|Быстрое|
|Изоляция|Нет изоляции|

👉 Процесс — контейнер ресурсов  
👉 Поток — единица выполнения

---

## 7. Процесс как основа мультипрограммирования

Мультипрограммирование достигается за счёт:

- быстрого переключения процессов
    
- таймера
    
- планировщика
    

ОС **не ускоряет CPU**, она **лучше его распределяет**.

---

## 8. Типичные экзаменационные формулировки

Можно говорить так:

> Процесс — это выполняемая программа, представленная в операционной системе в виде структуры данных, содержащей её контекст выполнения, состояние и выделенные ресурсы. Для управления процессами ОС использует модель состояний и блоки управления процессами (PCB), а выполнение нескольких процессов обеспечивается механизмом переключения контекста.

---

## 9. Связь с другими темами

Эта тема напрямую связана с:

- потоками
    
- IPC
    
- планированием
    
- взаимным исключением
    
- виртуальной памятью